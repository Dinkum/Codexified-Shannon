interface:
  display_name: "CS Report"
  short_description: "Builds the final local scan report"
  default_prompt: "Use $cs-report to synthesize the final report for reports/example-repo-2026-04-15."
policy:
  allow_implicit_invocation: true
